{% extends 'msqlfacoopBundle::layout.html.twig' %}

{% block title %}Editer un Domaine{% endblock %}

{% block content %}

<form action="{{ path('domaines_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
   <div class="error">
	{{ form_errors(edit_form) }}
	</div>
	<table>
	    <tr>
			<td>{{ form_label(edit_form.domaines) }}</td>
			<td>
			{{ form_errors(edit_form.domaines) }}
			{{ form_widget(edit_form.domaines) }}
			</td>
		</tr>
	</table>
	
	{{ form_rest(edit_form) }}
	
	<input type="submit" class="modifier" style="color:white"/>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('domaines') }}" class="retour" style="color:white">
            Retour à la liste
        </a>
    </li>
    <li>
        <form action="{{ path('domaines_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="supprimer" style="color:white">Delete</button>
        </form>
    </li>
</ul>


{% endblock %}