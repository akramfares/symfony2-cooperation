{% extends 'msqlfacoopBundle::layout.html.twig' %}

{% block title %}Liste des Domaines{% endblock %}

{% block content %}

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Domaines</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('domaines_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.domaines }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('domaines_show', { 'id': entity.id }) }}" class="retour" style="color:white">Afficher</a>
                    </li>
                    <li>
                        <a href="{{ path('domaines_edit', { 'id': entity.id }) }}" class="modifier" style="color:white">Editer</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('domaines_new') }}" class="ajouter" style="color:white">
            Ajouter
        </a>
    </li>
</ul>


{% endblock %}