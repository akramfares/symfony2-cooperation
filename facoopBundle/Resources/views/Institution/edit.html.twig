{% extends 'msqlfacoopBundle::layout.html.twig' %}

{% block title %}Editer une Institution{% endblock %}

{% block content %}


<form action="{{ path('institution_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
     <div class="error">
	{{ form_errors(edit_form) }}
	</div>
	<table>
	    <tr>
			<td>{{ form_label(edit_form.institution) }}</td>
			<td>
			{{ form_errors(edit_form.institution) }}
			{{ form_widget(edit_form.institution) }}
			</td>
		</tr>
		<tr>
			<td>{{ form_label(edit_form.nature) }}</td>
			<td>
			{{ form_errors(edit_form.nature) }}
			{{ form_widget(edit_form.nature) }}
			</td>
		</tr>
		<tr>
			<td>{{ form_label(edit_form.adresse) }}</td>
			<td>
			{{ form_errors(edit_form.adresse) }}
			{{ form_widget(edit_form.adresse) }}
			</td>
		</tr>
		<tr>
			<td>{{ form_label(edit_form.pays) }}</td>
			<td>
			{{ form_errors(edit_form.pays) }}
			{{ form_widget(edit_form.pays) }}
			</td>
		</tr>
	</table>
	
	{{ form_rest(edit_form) }}
	
	<input type="submit" class="modifier" style="color:white"/>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('institution') }}" class="retour" style="color:white">
            Retour à la liste
        </a>
    </li>
    <li>
        <form action="{{ path('institution_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="supprimer" style="color:white">Delete</button>
        </form>
    </li>
</ul>

{% endblock %}
